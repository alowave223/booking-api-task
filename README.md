
## Project setup

```bash
$ yarn install
```

## Compile and run the project

```bash
$ docker-compose up -d
$ yarn prisma init
$ yarn prisma migrate resolve --applied 0_init
$ yarn seed
$ yarn run start
```


